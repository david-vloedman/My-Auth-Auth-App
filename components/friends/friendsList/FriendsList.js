export default function FriendsList(props){
const {friendsList} = props
const getUserList = async () => { /* get all users */ }
const addFriend = async (uid) => { /* add friend */}
const removeFriend = async (uid) => {/* remove friend */ }

  return (
    <div></div>
  )
}