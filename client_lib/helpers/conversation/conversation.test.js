/**
 *
 *  SendMessage, LoadConversation, UnloadConversation, UpdateConversation, CreateConversation
 *
 *
 */
import {
	SendMessage,
	LoadConversation,
	UnloadConversation,
	UpdateConversation,
	CreateConversation,
} from 'client_lib/helpers/conversation/conversation'

/**
 *  Send message should
 */
describe(
	'Send message should receive a reference to dispatch and conversation state and a postMessage function',
  () => {
    it('should accept its parameters', () => {
      
    })
  }
)
